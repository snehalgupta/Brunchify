<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="teamcool.mandeep.brunchify">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="Activities.WaitingListActivity"></activity>
        <activity android:name="Activities.SelectOptions" />
        <activity android:name="Activities.DevSignUpActivity" />
        <activity android:name="Activities.WelcomeActivity" />
        <activity android:name="Activities.Dashboard" />
        <activity android:name="Activities.WeeklySignUp" />
        <activity android:name="Activities.InviteActivity" />
        <activity android:name="Activities.pastmeetingsactivity" />
        <activity android:name="Activities.upcomingmeetupsactivity" />
        <activity android:name="Activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>